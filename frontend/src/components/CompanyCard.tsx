import { Bookmark, BookmarkCheck, MapPin } from 'lucide-react';
import type { Company } from '@/data';

export default function CompanyCard({ company, onOpen, saved, onSave }: { company: Company; onOpen: (company: Company) => void; saved: boolean; onSave: (company: Company) => void }) {
  return <div className="result-row group" data-testid={`row-company-${company.id}`}><button data-testid={`button-open-company-${company.id}`} className="flex min-w-0 flex-1 items-center gap-3 text-left" onClick={() => onOpen(company)}><div className="company-mark">{company.initials}</div><span className="min-w-0"><span className="block truncate text-[12px] font-extrabold">{company.name}</span><span className="mt-1 block truncate text-[11px] text-[hsl(var(--muted-foreground))]">{company.industry} <span className="px-1 text-[hsl(var(--border))]">/</span> {company.companyType ?? 'Company'} <span className="px-1 text-[hsl(var(--border))]">/</span> {company.size} employees</span><span className="mt-1.5 flex items-center gap-1 text-[10px] text-[hsl(var(--muted-foreground))]"><MapPin size={10} />{company.location}</span></span></button><div className="ml-2 flex shrink-0 items-center gap-4"><div className="hidden text-right sm:block"><div className="font-mono text-[11px] font-bold text-[hsl(var(--primary))]">{company.relevanceScore ?? 80}<span className="text-[9px] font-normal text-[hsl(var(--muted-foreground))]"> / 100</span></div><div className="mt-1 text-[9px] text-[hsl(var(--muted-foreground))]">fit score</div></div><button data-testid={`button-save-company-${company.id}`} className={`save-button ${saved ? 'saved' : ''}`} onClick={() => onSave(company)} aria-label={saved ? `Remove ${company.name} from saved` : `Save ${company.name}`}>{saved ? <BookmarkCheck size={17} /> : <Bookmark size={17} />}</button></div></div>;
}