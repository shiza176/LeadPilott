import { Bookmark, BookmarkCheck, Building2, ExternalLink, Globe2, Link as LinkIcon, Linkedin, Mail, MapPin, Users, X } from 'lucide-react';
import type { Company, Lead } from '@/data';
import { Avatar } from '@/components/layout';

type Notice = { message: string; kind?: 'success' | 'default' };
type Detail = { kind: 'lead'; item: Lead } | { kind: 'company'; item: Company } | null;

function DetailLine({ icon: Icon, label, value }: { icon: typeof MapPin; label: string; value: string }) {
  return <div className="mb-3 flex items-start gap-3"><Icon size={14} className="mt-0.5 text-[hsl(var(--muted-foreground))]" /><div><p className="text-[10px] font-semibold text-[hsl(var(--muted-foreground))]">{label}</p><p className="mt-0.5 text-[12px] font-bold">{value}</p></div></div>;
}
function MiniStat({ value, label }: { value: string; label: string }) { return <div className="rounded-lg bg-[hsl(var(--muted)/.7)] p-3"><p className="text-[14px] font-extrabold">{value}</p><p className="mt-1 text-[9px] font-mono uppercase tracking-wide text-[hsl(var(--muted-foreground))]">{label}</p></div>; }

export default function DetailPanel({ detail, onClose, savedLeadIds, setSavedLeadIds, notify, companyCatalog }: { detail: Detail; onClose: () => void; savedLeadIds: string[]; setSavedLeadIds: (ids: string[]) => void; notify: (notice: Notice) => void; companyCatalog: Company[] }) {
  if (!detail) return null;
  const isLead = detail.kind === 'lead';
  const lead = isLead ? detail.item as Lead : null;
  const company = !isLead ? detail.item as Company : companyCatalog.find(item => item.id === (detail.item as Lead).companyId);
  const save = () => { if (!lead) return; const saved = savedLeadIds.includes(lead.id); setSavedLeadIds(saved ? savedLeadIds.filter(id => id !== lead.id) : [...savedLeadIds, lead.id]); notify({ message: saved ? `${lead.name} removed from saved` : `${lead.name} saved to your workspace`, kind: 'success' }); };
  return <div className="fixed inset-0 z-50 flex justify-end bg-[hsl(var(--foreground)/.18)] backdrop-blur-[2px]" onMouseDown={e => { if (e.target === e.currentTarget) onClose(); }}><aside className="detail-panel animate-rise scrollbar-thin h-full w-full overflow-y-auto bg-[hsl(var(--card))] shadow-2xl sm:w-[450px]"><div className="flex items-center justify-between border-b border-[hsl(var(--border))] px-5 py-4"><span className="eyebrow">{isLead ? 'Lead profile' : 'Company profile'}</span><button data-testid="button-close-detail" className="btn-quiet p-2" onClick={onClose}><X size={16} /></button></div><div className="p-5 md:p-7">{isLead && lead ? <><div className="flex items-start justify-between gap-3"><div className="flex items-center gap-3"><Avatar initials={lead.initials} tone="teal" size="lg" /><div><h2 className="text-[19px] font-extrabold tracking-[-.04em]">{lead.name}</h2><p className="mt-1 text-[12px] text-[hsl(var(--muted-foreground))]">{lead.title}</p></div></div><div className="score-big"><span>{lead.score}</span><small>fit</small></div></div><div className="mt-6 flex gap-2"><button data-testid="button-detail-save-lead" className={`btn-primary flex flex-1 items-center justify-center gap-2 ${savedLeadIds.includes(lead.id) ? 'saved-primary' : ''}`} onClick={save}>{savedLeadIds.includes(lead.id) ? <BookmarkCheck size={14} /> : <Bookmark size={14} />} {savedLeadIds.includes(lead.id) ? 'Saved' : 'Save lead'}</button><a data-testid="link-detail-linkedin" className="btn-outline flex items-center gap-2" href={`https://${lead.linkedin}`} target="_blank" rel="noreferrer"><Linkedin size={14} /> LinkedIn</a></div><div className="detail-section"><p className="eyebrow mb-3">At a glance</p><DetailLine icon={Building2} label="Company" value={lead.companyName} /><DetailLine icon={MapPin} label="Location" value={lead.location} /><DetailLine icon={Users} label="Department" value={`${lead.department} · ${lead.seniority}`} /><div className="mb-3 flex items-start gap-3"><Mail size={14} className="mt-0.5 text-[hsl(var(--muted-foreground))]" /><div><p className="text-[10px] font-semibold text-[hsl(var(--muted-foreground))]">Email</p><a data-testid="link-detail-email" className="mt-0.5 block text-[12px] font-bold text-[hsl(var(--primary))] hover:underline" href={`mailto:${lead.email}`}>{lead.email}</a></div></div></div><div className="detail-section"><p className="eyebrow mb-3">Why this lead</p><div className="rounded-lg bg-[hsl(var(--muted)/.7)] p-4"><p className="text-[12px] leading-6 text-[hsl(var(--foreground)/.78)]">This profile matches your brief through a relevant <strong>{lead.role.toLowerCase()}</strong> title, a strong location fit, and an active role at a company in your target market.</p></div></div><div className="detail-section"><p className="eyebrow mb-3">Links</p><a data-testid="link-detail-website" href={`https://${lead.website}`} target="_blank" rel="noreferrer" className="detail-link"><Globe2 size={14} /> {lead.website}<ExternalLink size={12} className="ml-auto" /></a><a data-testid="link-detail-profile" href={`https://${lead.linkedin}`} target="_blank" rel="noreferrer" className="detail-link"><LinkIcon size={14} /> Professional profile <ExternalLink size={12} className="ml-auto" /></a></div></> : company && <><div className="flex items-center gap-3"><div className="company-mark large">{company.initials}</div><div><h2 className="text-[19px] font-extrabold tracking-[-.04em]">{company.name}</h2><p className="mt-1 text-[12px] text-[hsl(var(--muted-foreground))]">{company.industry}</p></div></div><div className="mt-6 grid grid-cols-3 gap-2"><MiniStat value={company.peopleCount.toString()} label="people" /><MiniStat value={company.size} label="company size" /><MiniStat value={company.founded.toString()} label="founded" /></div><div className="detail-section"><p className="eyebrow mb-3">About the company</p><p className="text-[12px] leading-6 text-[hsl(var(--foreground)/.78)]">{company.description}</p></div><div className="detail-section"><p className="eyebrow mb-3">Company details</p><DetailLine icon={MapPin} label="Location" value={company.location} /><DetailLine icon={Users} label="People found" value={`${company.peopleCount} relevant profiles`} /></div><div className="detail-section"><p className="eyebrow mb-3">Links</p><a data-testid="link-company-website" href={`https://${company.website}`} target="_blank" rel="noreferrer" className="detail-link"><Globe2 size={14} /> {company.website} <ExternalLink size={12} className="ml-auto" /></a><a data-testid="link-company-linkedin" href={`https://${company.linkedin}`} target="_blank" rel="noreferrer" className="detail-link"><Linkedin size={14} /> Company LinkedIn <ExternalLink size={12} className="ml-auto" /></a></div></>}</div></aside></div>;
}
