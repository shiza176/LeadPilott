import { useState } from 'react';
import { Building2, Check, Mail, SlidersHorizontal } from 'lucide-react';
import { locations } from '@/data';
import { SectionHeader } from '@/components/layout';

type Notice = { message: string; kind?: 'success' | 'default' };

export default function SettingsPage({ notify }: { notify: (notice: Notice) => void }) {
  const [workspace, setWorkspace] = useState('Northstar Ventures');
  const [defaultLocation, setDefaultLocation] = useState('Pakistan');
  const [emailAlerts, setEmailAlerts] = useState(true);
  return <div className="animate-rise max-w-[930px]"><SectionHeader eyebrow="Workspace controls" title="Settings." description="Keep the workspace tuned to the way your team researches new opportunities." action={<button data-testid="button-save-settings" className="btn-primary flex items-center gap-2" onClick={() => notify({ message: 'Settings saved', kind: 'success' })}><Check size={14} /> Save changes</button>} /><div className="grid gap-5 lg:grid-cols-[190px_1fr]"><nav className="hidden space-y-1 lg:block"><div className="settings-nav active">Workspace</div><div className="settings-nav">Preferences</div><div className="settings-nav">Notifications</div></nav><div className="space-y-4"><section className="card p-5 md:p-6"><div className="mb-6 flex items-start gap-3"><div className="settings-icon"><Building2 size={16} /></div><div><h2 className="text-[14px] font-extrabold">Workspace identity</h2><p className="mt-1 text-[11px] text-[hsl(var(--muted-foreground))]">This is how your workspace appears to teammates.</p></div></div><div className="grid gap-4 sm:grid-cols-2"><label><span className="eyebrow mb-2 block">Workspace name</span><input data-testid="input-workspace-name" className="field text-[12px]" value={workspace} onChange={e => setWorkspace(e.target.value)} /></label><label><span className="eyebrow mb-2 block">Your role</span><select data-testid="select-workspace-role" className="field text-[12px]" defaultValue="Founder"><option>Founder</option><option>Revenue leader</option><option>Researcher</option><option>Advisor</option></select></label></div></section><section className="card p-5 md:p-6"><div className="mb-6 flex items-start gap-3"><div className="settings-icon orange"><SlidersHorizontal size={16} /></div><div><h2 className="text-[14px] font-extrabold">Discovery defaults</h2><p className="mt-1 text-[11px] text-[hsl(var(--muted-foreground))]">Pre-fill the signals you reach for most often.</p></div></div><div className="grid gap-4 sm:grid-cols-2"><label><span className="eyebrow mb-2 block">Default location</span><select data-testid="select-default-location" className="field text-[12px]" value={defaultLocation} onChange={e => setDefaultLocation(e.target.value)}>{locations.slice(0, 6).map(location => <option key={location}>{location}</option>)}</select></label><label><span className="eyebrow mb-2 block">Results per page</span><select data-testid="select-results-page" className="field text-[12px]" defaultValue="25"><option>25 results</option><option>50 results</option><option>100 results</option></select></label></div></section><section className="card p-5 md:p-6"><div className="mb-5 flex items-start gap-3"><div className="settings-icon blue"><Mail size={16} /></div><div><h2 className="text-[14px] font-extrabold">Notifications</h2><p className="mt-1 text-[11px] text-[hsl(var(--muted-foreground))]">Choose when LeadPilot should nudge you.</p></div></div><div className="flex items-center justify-between rounded-lg border border-[hsl(var(--border))] p-3.5"><div><p className="text-[12px] font-bold">Weekly research digest</p><p className="mt-1 text-[10px] text-[hsl(var(--muted-foreground))]">A short summary of saved leads and new signals.</p></div><button data-testid="button-toggle-email-alerts" className={`switch ${emailAlerts ? 'on' : ''}`} onClick={() => setEmailAlerts(!emailAlerts)} aria-label="Toggle weekly research digest"><span /></button></div></section><p className="px-1 text-[10px] leading-5 text-[hsl(var(--muted-foreground))]">LeadPilot prototype · Your demo data stays in this browser session. API connections can be added when your workspace is ready.</p></div></div></div>;
}
